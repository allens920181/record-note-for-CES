# 神學院錄音筆記軟體 — 需求規格 v0.7

> 狀態：**需求已鎖定，可開工。** 七項待拍板全部有答案。

---

## 0. 一句話定位

給神學院學生的課堂工具：**錄音 → 逐字稿 → 雙欄筆記 → 整學期知識庫**。
資料存在自己的電腦，轉錄用自己的 API key，不經過任何第三方伺服器。
**摘要與整理由你自己做**，軟體不代勞。

---

## 1. 已確認的決定

| 項目 | 決定 |
|---|---|
| 平台形式 | 純網頁 local-first，靜態站 |
| 目標瀏覽器 | **Chrome**（可用 File System Access API） |
| 儲存方式 | **選一個本機資料夾**，音檔與 PDF 存硬碟、DB 只存索引 |
| 錄音來源 | 事後上傳音檔 ＋ App 內直接錄音 |
| 授課語言 | **中文授課，內容含希臘文／希伯來文術語** |
| 規模 | **每學期約 21 學分** |
| 講者處理 | **不做自動分離**。講者名稱在課程層手動設定（例：本課教授＝OOO） |
| 建立方式 | **以週曆／月曆為主**。點日曆空白處建立，重複方式在建立時選 |
| 課表 | 只放**每週固定**的聚會：正課與分組討論，各自每週開一個檔案 |
| 單次聚會 | 只開一次的分組討論或補課，直接**指定日期**新增，不寫進課表 |
| 作業時間 | **獨立於課表**，不產生任何檔案。可每週固定，也可為某份報告單獨空出一天 |
| AI 文字處理 | **完全不做。** 摘要、大綱、複習包、作業拆解一律自己來，軟體只負責轉錄與檢索 |
| 錄音加密 | 不需要 |
| 手機版 | 不需要 |
| 課程串聯 | 全學期總覽 ＋ 跨週全文搜尋（純本地檢索，不經 AI） |
| 首版範圍 | 錄音筆記、學期結構、PDF 與閱讀材料、作業規劃（四大塊全做） |

---

## 2. 規模與成本試算

### 2.1 實際用量

> 假設：1 學分 ＝ 每週 1 小時課，一學期 15 週。**若你的學制不同請告訴我，數字會等比例變動。**

| 指標 | 數值 |
|---|---|
| 每週上課時數 | 21 小時 |
| 一學期音訊總長 | **315 小時**（18 週制為 378 小時） |
| 平均每天 | 3.0 小時 |
| 逐字稿字數 | 約 378 萬字／學期 |
| 每學期堂數 | 約 105 堂 |

### 2.2 轉錄成本比較（315 小時）

| 方案 | $/小時 | 一學期（15 週） | 一學期（18 週） |
|---|---:|---:|---:|
| **Groq 免費層**（每日 8 小時內） | **$0** | **$0** | **$0** |
| Groq `whisper-large-v3-turbo` | $0.040 | $12.60 | $15.12 |
| **Groq `whisper-large-v3`** ← 建議 | $0.111 | $34.97 | $41.96 |
| OpenAI `gpt-4o-mini-transcribe` | $0.180 | $56.70 | $68.04 |
| Gemini 3.5 Transcribe | $0.300 | $94.50 | $113.40 |
| Deepgram Nova-3（多語） | $0.312 | $98.28 | $117.94 |
| OpenAI `whisper-1` / `gpt-4o-transcribe` | $0.360 | $113.40 | $136.08 |

### 2.3 儲存需求

| 音質 | 每小時 | 一學期 |
|---|---:|---:|
| opus 32 kbps mono（語音足夠） | 14.4 MB | **4.5 GB** |
| opus 64 kbps mono | 28.8 MB | 9.1 GB |

> 4.5 GB 遠超瀏覽器 IndexedDB 配額。這再次確認「本機資料夾」是必要而非選配。

### 2.4 結論

轉錄是**唯一**的經常性支出，因為沒有任何 LLM 呼叫。

| 走法 | 一學期 |
|---|---:|
| Groq 免費層（每日 8 小時內） | **$0** |
| 超額改走付費 `whisper-large-v3` | **最多 $34.97** |

**一學期 $0 – $35 美金。** 大多數月份應該是 $0。

---

## 3. STT 方案決定：Groq 上的 Whisper large-v3

### 3.1 「只要用 Whisper 就好嗎？」— 是，但要挑對託管商與變體

**用 Whisper 的三個理由：**

1. **`prompt` 參數可餵詞彙表。** 這是決定性的。你的課有希臘文／希伯來文術語和神學家人名，通用模型必錯。Whisper 的 API 讓你把詞彙表塞進 `prompt`，直接拉高這些詞的命中率。
2. **中文表現穩定**，large-v3 是目前開源多語模型裡最可靠的一梯。
3. **你不需要講者分離**（已確認手動設定講者名稱），所以 Deepgram 最大的賣點對你沒有價值 → 直接排除，省一半錢。

### 3.2 為什麼是 Groq 而不是 OpenAI

同一個 Whisper 模型，Groq 便宜 3.2 倍（$0.111 vs $0.36），而且跑在 LPU 上，一小時音訊約 15 秒轉完。

**Groq 免費層對你的用量幾乎剛好夠：**

| 限制 | 額度 | 你的用量 |
|---|---|---|
| 每日音訊 | 28,800 秒（8 小時） | 平均 3 小時／天 ✅ |
| 每小時音訊 | 7,200 秒（2 小時） | 一堂 3 小時的課需約 1.5 小時處理 ⚠️ |
| 每日請求 | 2,000 次 | 遠遠夠用 ✅ |
| 每分鐘請求 | 20 次 | 需要節流 ⚠️ |

→ **設計對策：轉錄工作以佇列處理，顯示進度，可掛著跑。** 不追求即時。
→ 提供「付費加速」開關：超過免費額度時自動改走付費，或手動切換。

### 3.3 為什麼是 large-v3 而不是 turbo

turbo 是 large-v3 的剪枝微調版：解碼層從 32 降到 4，參數從 1.54B 降到 809M。速度快很多，代價是品質下降——而且**退化集中在非英語**，粵語、泰語等尤其明顯。

你的場景正是它最會出錯的地方：中文授課 ＋ 外語術語。

而且**在免費層底下，兩個模型的額度完全相同**（20 RPM / 2,000 RPD / 7,200 音訊秒每小時 / 28,800 每日）。既然免費層不用多付錢，就直接用準確度高的那個。就算超額付費，一學期也只差 $22。

### 3.4 備援設計

Groq 用的是 **OpenAI 相容端點**，所以「Groq」與「OpenAI」在程式裡是同一條路徑，只差 base URL 和 key。

→ 設定頁預設 Groq，保留 OpenAI 作為第二來源。某段轉壞或 Groq 出狀況時可一鍵改用另一家重試該段。這在架構上幾乎零成本。

### 3.5 待驗證

- **CORS。** Groq 與 OpenAI 是否允許瀏覽器直接呼叫，尚未實測（開發沙盒的網路政策擋住了這些網域）。**這是開工前第一件要在 Chrome devtools 上驗的事。** 若不支援，備案是加一層 Cloudflare Worker proxy。
- **免費層資料條款。** 課堂錄音上傳免費層前，值得確認 Groq 對免費層資料的使用政策，以及學校對課程錄音的規定。
- 費率與免費層額度可能變動，以官網現價為準。

---

## 4. 資料模型

```
Term 學期  ── 名稱、起訖日、週數（15 或 18）、狀態
 └── Course 課程 ── 課名、教授、學分、課號、代表色
      ├── 上課時段 ── 類型 / 星期幾 / 幾點 / 教室（可多段）
      │                僅每週固定的聚會：正課 · 分組討論
      ├── WorkBlock 作業時間 ── 每週固定 或 單次指定日期
      │                不產生檔案，供作業規劃回推可用時數
      ├── Speakers  講者名稱 ← 手動設定，如 教授=OOO、助教=OOO
      ├── Syllabus  教學大綱（PDF）
      ├── Glossary  專有名詞表 ← 見 §5.2
      │
      ├── Session 週次  ← 依課表自動預生成（僅正課與分組討論）
      │    ├── 第 N 週、類型、日期、主題、是否停課
      │    ├── Recording  錄音（可多段，含分段續錄）
      │    ├── Transcript 逐字稿（帶時間戳，可編輯修正）
      │    ├── Note       我的筆記（Markdown，可插時間戳）
      │    └── Attachment 當週講義
      │
      ├── Assignment 作業 ── 標題、截止日、狀態、關聯週次
      │    └── SubTask 子任務（作業規劃）
      │
      └── Reading 閱讀材料 ── 書目、頁數範圍、對應週次、進度、讀後筆記
```

---

## 5. 功能規格

### 5.1 錄音與轉錄

- **上傳**：mp3 / m4a / wav / webm / mp4，拖放即可
- **內建錄音**：每 3–5 分鐘落一塊到本機資料夾、Wake Lock 防休眠、暫停續錄、即時波形計時
- **轉錄管線**：壓成 opus 32 kbps mono → 依靜音點切片（每片 &lt; 25 MB）→ 佇列送 Groq（節流至 20 RPM 內）→ 時間戳偏移校正 → 拼接
- **韌性**：每片獨立進度，單片失敗可重試或改用備援 provider，不必整份重來
- **成本顯示**：送出前顯示「約 X 分鐘，免費額度剩 Y 小時，預估 $Z」
- **匯入現成逐字稿**：srt / vtt / txt

### 5.2 專有名詞表 — 這個場景的關鍵功能

中文授課夾雜希臘文／希伯來文術語，通用 STT 必錯。透過 Whisper 的 `prompt` 參數餵詞彙表可大幅改善。

- 每門課一份詞彙表
- 可手動維護，也可從你的逐字稿修正紀錄自動累積（改過一次「加爾聞 → 加爾文」就記住）
- 內建一份神學基礎詞庫作為起點（神學家人名、宗派術語、常見原文音譯）

### 5.3 雙欄：逐字稿 ↔ 我的筆記

| 左欄：逐字稿 | 右欄：我的筆記 |
|---|---|
| 帶時間戳，跟著音檔捲動 | Markdown 編輯器 |
| 可直接編輯修正錯字（回饋進詞彙表） | 可插入時間戳連結 |
| 可 highlight 標記 | 可引用左欄選取段落 |
| 可標示講者（用課程層設定的名稱） | 可連到 PDF 頁碼 |

**核心價值：筆記裡的時間戳點下去，音檔直接跳到那一秒。**
上課按快捷鍵記一筆「這裡沒聽懂」，回家點一下就回到那個瞬間。

**軟體不提供任何 AI 文字整理功能。** 摘要、大綱、重點、複習問題都由你自己在右欄寫——那正是讀神學的功課本身。軟體的職責是讓你「找得到、跳得回去、寫得順」。

### 5.4 行事曆與自動開檔

**日曆是建立行程的主要方式。**一旦分組討論與作業時間都可能是不規則的，
用下拉選單填表就是錯的工具：看不到整週的形狀，也看不出衝堂。

| 視圖 | 用途 |
|---|---|
| **週曆** | 7 天 × 時間格。衝堂的項目並排顯示而非互相遮蓋。這是排課與看一週輕重的地方 |
| **月曆** | 一眼看完整學期的節奏與密度 |

**點空白處建立**：帶出課程、類型（正課／分組討論／作業時間）、重複（每週／單次），
時間依點擊位置預填並對齊半小時。三種組合的行為不同：

- 每週 ＋ 會議 → 寫進課表並展開整學期
- 單次 ＋ 會議 → 只建立那一天的一個週次，不影響課表
- 作業時間 → 建立時段，不產生任何檔案

其餘規則：

- 同一週的正課與分組討論共用同一個週次編號，列表依日期排序
- 週次編號以學期起始日往後每 7 天為一個窗口推算，刪掉或補開都能重算
- 點既有項目直接開啟那一週的工作區
- 學期尚未開始時，日曆停在學期第一週而不是空白的今天
- 可標記停課、放假週

### 5.5 PDF 與課程檔案

- 上傳 syllabus、講義、閱讀材料，掛課程層或週次層
- 內建 PDF 檢視器（pdf.js）
- 抽出文字讓 PDF 可被跨週全文搜尋

### 5.6 作業清單與規劃

- 清單 ＋ 截止日 ＋ 狀態 ＋ 關聯課程／週次
- 子任務拆解（選題 / 找文獻 / 大綱 / 初稿 / 修稿）
- 時間軸視圖，看見死線擠在一起
- 可存常用的子任務範本（如「期末報告」五步驟），重複套用
- **用作業時間回推可用時數**：截止日之前實際還剩幾小時能寫，比單看死線更接近真實情況。
  每週固定的時段乘以剩餘週數，加上單次挪出來的那幾天

### 5.7 閱讀材料區

- 書目管理：書名、作者、章節、頁數範圍
- 對應到週次
- 進度追蹤：未讀 / 讀到第幾頁 / 已讀
- 讀書筆記，可與課堂筆記互相引用

### 5.8 整學期串聯

- **課程時間軸總覽**：一門課的所有週次攤開，主題、錄音狀態、筆記狀態一目了然
- **跨週全文搜尋**：一次搜尋逐字稿 ＋ 筆記 ＋ PDF 文字，結果顯示「第 8 週 01:23:45」，點下去直接跳到那句話
- **自己的筆記索引**：你在筆記裡下的標籤與標題，彙成一份可跳轉的全課大綱

串聯**完全是本地的導航與檢索**，沒有任何 AI 呼叫。整學期的複習素材由搜尋與你自己的筆記構成——
搜尋帶你回到原話，筆記是你的理解，兩者互為索引。

### 5.9 儲存與備份

- **主要模式：本機資料夾**（File System Access API，Chrome）。音檔與 PDF 直接存硬碟，資料庫只存索引與文字。
- 一鍵匯出 zip 備份，含 Markdown，可餵給 Obsidian
- 可匯入還原、可換電腦
- 「轉錄完成後自動刪除音檔」選項——但預設**保留**，因為時間戳跳轉需要音檔
- API key 存本機 IndexedDB（已確認不需加密）

---

## 6. 交付階段

每個階段結束都是能實際拿去上課用的狀態。

| Phase | 內容 | 做完後你能做什麼 |
|---|---|---|
| **1** | 資料模型、學期/課程/週次、本機資料夾儲存、上傳音檔 → 切片 → Groq 轉錄 → 雙欄筆記、時間戳跳轉 | 課後上傳錄音，做出可點跳的雙欄筆記 |
| **2** | 內建錄音（分段落磁碟 ＋ Wake Lock）、課表自動開檔、PDF 上傳與檢視、**專有名詞表** | 帶筆電直接上課錄，整學期檔案自動排好，術語轉得準 |
| **3** | 作業清單與規劃、閱讀材料區、跨週全文搜尋、課程時間軸總覽 | 學期的所有事情都在同一個地方 |
| **4** | 詞彙表自動累積、匯出 Obsidian、用量與免費額度儀表板 | 用久了越轉越準，資料隨時帶得走 |

---

## 7. 技術選型

| 層 | 選擇 | 理由 |
|---|---|---|
| 框架 | Vite + React + TypeScript | 靜態輸出、生態成熟 |
| 資料庫 | Dexie（IndexedDB 封裝） | local-first 標準解 |
| 檔案 | File System Access API | 4.5 GB／學期，非用不可 |
| 音訊處理 | `ffmpeg.wasm` | 壓縮與切片全在瀏覽器完成，不外傳 |
| STT | Groq `whisper-large-v3`（OpenAI 相容端點） | 準確度／價格最佳解，OpenAI 作備援 |
| PDF | `pdf.js` | 檢視 ＋ 抽文字 |
| 編輯器 | TipTap 或 CodeMirror | Markdown ＋ 自訂時間戳節點 |
| 全文搜尋 | MiniSearch 或 SQLite WASM (FTS5) | 跨週搜尋 |
| 部署 | GitHub Pages 靜態站 | 零成本、零維運 |

---

## 附錄：資料來源

轉錄費率查證於 2026-08-27，以各家官網現價為準：

- OpenAI 費率 — [ConvertAudioToText](https://convertaudiototext.com/blog/openai-whisper-api-pricing-2026)、[DIY AI](https://diyai.io/ai-tools/speech-to-text/openai-whisper-api-pricing-2026/)
- Groq 費率與免費層 — [ComputePrices](https://computeprices.com/providers/groq/models/whisper-v3-turbo)、[Grizzly Peak Software](https://www.grizzlypeaksoftware.com/articles/p/groq-api-free-tier-limits-in-2026-what-you-actually-get-uwysd6mb)、[eesel AI](https://www.eesel.ai/blog/groq-pricing)
- turbo 品質退化 — [openai/whisper-large-v3-turbo · Hugging Face](https://huggingface.co/openai/whisper-large-v3-turbo)
- Deepgram 費率 — [ConvertAudioToText](https://convertaudiototext.com/blog/deepgram-nova-3-explained)
- Gemini 費率 — [OrcaRouter](https://www.orcarouter.ai/blog/gemini-3-5-transcribe-intelligent-transcription)
